<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tutorial.dal.OrderDao">
    <insert id="saveOrder">
        insert into OrderLog(orderId, executionDate, orderType, executionPrice, symbol, userId)
          values (
            #{orderId}, #{executionDate}, #{orderType}, #{executionPrice}, #{symbol}, #{userId}
          )
    </insert>

    <select id="getOrders" resultType="tutorial.om.Order">
        SELECT * FROM OrderLog;
    </select>
</mapper>